# 今日计划看板404错误修复

## 错误信息

```
GET http://10.100.111.2:8675/parking/visitor/plan-dashboard/statistics 404 (Not Found)
❌ [今日计划看板] 加载统计数据失败: AxiosError
```

## 问题原因

后端Controller (`VisitorPlanDashboardController.java`) 已经创建，但后端服务**没有重启**，导致新的API接口没有被加载。

## 解决方案

### 方案1：重启后端服务（推荐）

#### IntelliJ IDEA中

1. 停止当前运行的应用（点击红色停止按钮）
2. 重新运行 `ParkingManageApplication` 主类
3. 等待Spring Boot启动完成

#### Maven命令行

```bash
# 停止当前服务（Ctrl+C）
cd d:\nefu-edu-datav\boot-new-20251112

# 清理并重新编译
mvn clean compile

# 启动服务
mvn spring-boot:run
```

### 方案2：使用热部署（可选）

如果经常修改代码，可以配置Spring Boot DevTools实现热部署。

**添加依赖**（`pom.xml`）：
```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-devtools</artifactId>
    <scope>runtime</scope>
    <optional>true</optional>
</dependency>
```

**配置**（`application.yml`）：
```yaml
spring:
  devtools:
    restart:
      enabled: true
```

## 验证步骤

### 步骤1：检查后端服务日志

启动后端服务后，查看控制台日志，应该能看到：

```
Mapped "{[/parking/visitor/plan-dashboard/statistics],methods=[GET]}" onto ...
Mapped "{[/parking/visitor/plan-dashboard/detail-list],methods=[GET]}" onto ...
```

### 步骤2：测试API接口

**方法1：浏览器直接访问**
```
http://10.100.111.2:8675/parking/visitor/plan-dashboard/statistics
```

**预期响应**：
```json
{
  "code": "0",
  "msg": "成功",
  "data": {
    "visitorStats": {
      "total": 0,
      "completed": 0,
      "pending": 0,
      "percentage": "0.0"
    },
    "vehicleStats": {
      "expected": 0,
      "arrived": 0,
      "current": 0
    },
    "dataSource": "DATABASE",
    "updateTime": "2025-01-17T17:45:00"
  }
}
```

**方法2：使用Postman**

- URL: `http://10.100.111.2:8675/parking/visitor/plan-dashboard/statistics`
- Method: GET
- Headers: 无需设置

**方法3：使用curl**

```bash
curl http://10.100.111.2:8675/parking/visitor/plan-dashboard/statistics
```

### 步骤3：刷新前端页面

1. 按 `Ctrl + F5` 强制刷新浏览器页面
2. 打开浏览器控制台（F12）
3. 查看日志输出

**成功日志示例**：
```
📊 [今日计划看板] 开始加载统计数据...
📡 [今日计划看板] API响应: {code: "0", ...}
✅ [今日计划看板] 数据加载成功 {访客: "0/0", 车辆: "0/0"}
```

### 步骤4：验证水波图

- 左侧："计划访客" 水波图应该显示数字
- 右侧："预计车辆" 水波图应该显示数字
- 如果数据库中有预约记录，水波图应该有填充

## 常见问题

### Q1：重启后还是404怎么办？

**检查清单**：

1. **端口是否正确**：
   ```bash
   # Windows
   netstat -ano | findstr "8675"
   
   # 应该看到有进程监听8675端口
   ```

2. **Controller类是否在正确位置**：
   ```
   d:\nefu-edu-datav\boot-new-20251112\src\main\java\com\parkingmanage\controller\VisitorPlanDashboardController.java
   ```

3. **包名是否正确**：
   ```java
   package com.parkingmanage.controller;
   ```

4. **注解是否正确**：
   ```java
   @RestController
   @RequestMapping("/parking/visitor/plan-dashboard")
   ```

### Q2：数据都是0怎么办？

这是正常的！如果数据库中没有符合条件的预约记录，统计数据就是0。

**插入测试数据**：

```sql
INSERT INTO visitor_reservation_sync (
  reservation_id, visitor_name, visitor_phone, car_number,
  start_time, end_time, pass_name, pass_dep,
  person_visit_status, car_visit_status, deleted
) VALUES
('TEST001', '张三', '13800138001', '京A12345',
 DATE_ADD(NOW(), INTERVAL -1 HOUR), 
 DATE_ADD(NOW(), INTERVAL 2 HOUR),
 '李四', '技术部', 
 '人已进场', '已进场', 0);
```

插入数据后，等待30秒或刷新页面，应该能看到数据更新。

### Q3：CORS跨域错误怎么办？

如果看到CORS错误，需要配置后端允许跨域请求。

**检查是否有CORS配置**：

```java
@Configuration
public class CorsConfig {
    @Bean
    public CorsFilter corsFilter() {
        CorsConfiguration config = new CorsConfiguration();
        config.addAllowedOrigin("http://localhost:8080");
        config.addAllowedOrigin("http://127.0.0.1:8080");
        config.setAllowCredentials(true);
        config.addAllowedMethod("*");
        config.addAllowedHeader("*");
        
        UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource();
        source.registerCorsConfiguration("/**", config);
        
        return new CorsFilter(source);
    }
}
```

## 完整测试流程

### 1. 停止前端和后端服务

### 2. 重启后端服务

```bash
cd d:\nefu-edu-datav\boot-new-20251112
mvn clean spring-boot:run
```

等待看到：
```
Tomcat started on port(s): 8675 (http)
Started ParkingManageApplication in X.XXX seconds
```

### 3. 验证后端API

浏览器访问：`http://10.100.111.2:8675/parking/visitor/plan-dashboard/statistics`

应该看到JSON响应（即使数据为0也是成功的）。

### 4. 启动前端服务

```bash
cd d:\nefu-edu-datav\vue-big-screen-master
npm run serve
```

### 5. 访问前端页面

浏览器访问：`http://localhost:8080`

找到"今日计划看板"组件，应该能看到水波图显示。

### 6. 检查控制台日志

按F12打开控制台，应该看到成功日志，没有404错误。

## API接口文档

### 1. 获取统计数据

**接口**: `GET /parking/visitor/plan-dashboard/statistics`

**说明**: 获取今日计划访客和车辆的统计数据

**参数**: 无

**响应**: 
```json
{
  "code": "0",
  "msg": "成功",
  "data": {
    "visitorStats": {
      "total": 45,          // 计划访客总数
      "completed": 32,      // 已来访人数
      "pending": 13,        // 待来访人数
      "percentage": "71.1"  // 来访率
    },
    "vehicleStats": {
      "expected": 28,   // 预计车辆数
      "arrived": 20,    // 已到车辆数
      "current": 8      // 待到车辆数
    },
    "dataSource": "DATABASE",
    "updateTime": "2025-01-17T17:30:00"
  }
}
```

### 2. 获取详细列表

**接口**: `GET /parking/visitor/plan-dashboard/detail-list`

**说明**: 获取访客或车辆的详细预约列表

**参数**:
- `type` (可选): `visitor`-访客列表, `vehicle`-车辆列表, 默认`visitor`

**示例**: 
```
GET /parking/visitor/plan-dashboard/detail-list?type=visitor
GET /parking/visitor/plan-dashboard/detail-list?type=vehicle
```

**响应**: 
```json
{
  "code": "0",
  "msg": "成功",
  "data": {
    "list": [
      {
        "id": 1,
        "visitorName": "张三",
        "visitorPhone": "13800138000",
        "carNumber": "京A12345",
        "startTime": "2025-01-17T08:00:00",
        "endTime": "2025-01-17T18:00:00",
        "passName": "李四",
        "passDep": "技术部",
        "personVisitStatus": "人已进场",
        "carVisitStatus": "已进场"
      }
    ],
    "total": 1,
    "type": "visitor"
  }
}
```

## 总结

### ✅ 问题原因
- 新创建的Controller没有被加载
- 需要重启后端服务

### ✅ 解决方案
- 重启Spring Boot应用
- 验证API接口可访问
- 刷新前端页面

### ✅ 验证方法
- 浏览器访问API地址
- 查看后端启动日志
- 检查前端控制台日志

---

**修复完成时间**：2025-01-17  
**问题类型**：部署问题（非代码BUG）  
**解决时间**：< 1分钟（重启服务）
