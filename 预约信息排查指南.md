# 预约信息不显示排查指南

## 问题现象
人脸监控详情表格中的"预约信息"列全部显示为"-"

## 排查步骤

### 1. 检查后端服务是否重启 ✅
修改后端代码后必须重启Spring Boot应用。

```bash
# 停止当前服务
# 重新启动服务
```

### 2. 检查浏览器控制台日志 🔍

打开浏览器开发者工具（F12），查看Console标签页中的日志：

#### 查找API请求日志
```
🌐 [API请求] 人脸数据: http://10.100.111.2:8675/parking/face-monitor/list
```

#### 查找API响应日志
```
📦 [API响应] 原始响应: { code: "0", data: { records: [...] } }
📦 [API响应] 第一条记录示例: { personName: "...", reservationTimeRange: "...", ... }
```

#### 查找预约信息调试日志
```
🔍 [预约信息调试] {
  isReservedVisitor: true/false,
  isPureVisitor: true/false,
  reservationTimeRange: "...",
  reservationFormName: "...",
  reservationCarPlate: "...",
  原始数据: { ... }
}
```

**如果这些字段都是 `null` 或空字符串，说明数据库中没有预约数据。**

### 3. 检查数据库字段 🗄️

**字段映射关系**：
- `reservation_time_range` → 预约时间段
- `vip_type_name` → 表单名称（预约表单）
- `plate_number` → 车牌号码（访客携带的车牌）

连接到MySQL数据库，执行以下查询：

```sql
-- 检查acms_event_record表结构
DESC acms_event_record;

-- 查看是否有预约数据
SELECT 
    person_name,
    reservation_time_range,
    vip_type_name,
    plate_number,
    event_type,
    event_time
FROM acms_event_record
WHERE reservation_time_range IS NOT NULL
   OR vip_type_name IS NOT NULL
   OR plate_number IS NOT NULL
LIMIT 10;

-- 统计预约数据
SELECT 
    COUNT(*) as total_records,
    COUNT(reservation_time_range) as has_time_range,
    COUNT(vip_type_name) as has_vip_type,
    COUNT(plate_number) as has_plate_number
FROM acms_event_record;
```

**预期结果**：
- 如果 `has_time_range`、`has_vip_type`、`has_plate_number` 都是 0，说明数据库中没有预约信息

### 4. 检查ACMS推送数据 📡

预约信息需要在ACMS推送事件数据时填充。检查ACMS推送的数据是否包含预约信息：

```sql
-- 查看最近的ACMS事件记录
SELECT 
    id,
    person_name,
    event_time,
    event_type,
    reservation_time_range,
    raw_data
FROM acms_event_record
ORDER BY event_time DESC
LIMIT 10;
```

查看 `raw_data` 字段中是否包含预约相关的JSON数据。

### 5. 测试数据插入 🧪

如果数据库中确实没有预约数据，可以手动插入测试数据：

```sql
-- 插入测试数据（预约访客）
UPDATE acms_event_record
SET 
    reservation_time_range = '2025-11-25 14:00:00-2025-11-25 16:00:00',
    vip_type_name = '访客预约申请',
    plate_number = '京A12345'
WHERE id = (
    SELECT id FROM acms_event_record 
    WHERE event_type = 197162  -- 人证比对
    ORDER BY event_time DESC 
    LIMIT 1
);

-- 验证更新
SELECT 
    person_name,
    reservation_time_range,
    vip_type_name,
    plate_number
FROM acms_event_record
WHERE reservation_time_range IS NOT NULL
LIMIT 1;
```

更新后，刷新前端页面，应该能看到该条记录显示预约信息。

### 6. 检查API响应格式 📄

在浏览器控制台的Network标签页中：

1. 找到 `face-monitor/list` 请求
2. 点击查看Response
3. 确认返回数据中包含以下字段：
   ```json
   {
     "code": "0",
     "data": {
       "records": [
         {
           "personName": "张三",
           "isReservedVisitor": true,
           "isPureVisitor": false,
           "reservationTimeRange": "2025-11-25 14:00:00-2025-11-25 16:00:00",
           "reservationFormName": "访客预约申请",
           "reservationCarPlate": "京A12345",
           "phoneNo": "138****5678",
           "idNumber": "110101********1234",
           ...
         }
       ],
       "total": 100,
       "page": 1,
       "size": 20
     }
   }
   ```

## 预期行为

### 预约访客
- **条件**：`isReservedVisitor = true`
- **显示**：`时间：2025-11-25 14:00:00-2025-11-25 16:00:00 | 表单：访客预约申请 | 车牌：京A12345`
- **样式**：绿色背景，左侧绿色边框

### 纯访客
- **条件**：`isPureVisitor = true`（event_type = 197162 且无预约信息）
- **显示**：`纯访客（未预约）`
- **样式**：橙色背景，左侧橙色边框

### 其他人员
- **条件**：非访客（教职工、学生等）
- **显示**：`-`
- **样式**：灰色文字

## 常见问题

### Q1: 所有记录都显示"-"
**原因**：数据库中 `reservation_time_range`、`vip_type_name`、`plate_number` 字段都是NULL

**解决**：
1. 检查ACMS推送数据是否包含预约信息
2. 检查 `AcmsVipController` 的 `eventRcv` 方法是否正确解析并保存预约信息
3. 使用测试数据验证功能

### Q2: 后端返回数据中字段为null
**原因**：实体类中没有对应的getter方法，或者字段名不匹配

**解决**：
1. 确认 `AcmsEventRecord.java` 中有 `vipTypeName` 和 `plateNumber` 字段
2. 确认使用了 `@Data` 注解或手动添加了getter方法
3. 数据库字段名应为 `vip_type_name` 和 `plate_number`（已存在）

### Q3: 前端显示undefined
**原因**：前端字段名和后端返回的不匹配

**解决**：检查 `transformDetailData` 方法中的字段映射是否正确

## 验证清单

- [ ] 后端服务已重启
- [ ] 浏览器已刷新（Ctrl+Shift+R 强制刷新）
- [ ] 控制台没有JavaScript错误
- [ ] Network中API返回200状态码
- [ ] API响应中包含预约相关字段
- [ ] 数据库中有预约数据
- [ ] 实体类包含新增字段

## 下一步

如果以上步骤都正常，但仍然不显示：

1. 截图控制台日志（包括 🔍 预约信息调试 的完整输出）
2. 截图Network中的API响应
3. 提供数据库查询结果
