# ✅ 部署清单 - 访客与VIP统计分析模块改版

## 📅 部署信息

- **部署日期：** _______________
- **部署人员：** _______________
- **环境：** □ 测试环境  □ 生产环境
- **版本号：** v2.0.0

---

## 📋 部署前准备

### 1. 环境检查

- [ ] JDK 8+ 已安装
- [ ] MySQL 5.7+ 运行正常
- [ ] Node.js 已安装（前端）
- [ ] 磁盘空间充足（>10GB）
- [ ] 内存充足（>4GB）

### 2. 数据库准备

- [ ] 数据库连接正常
- [ ] 备份现有数据
  ```bash
  mysqldump -u root -p parking_db > backup_$(date +%Y%m%d).sql
  ```
- [ ] 检查数据表存在
  ```sql
  SHOW TABLES LIKE 'report_car_%';
  ```
- [ ] 检查数据完整性
  ```sql
  SELECT COUNT(*) FROM report_car_in WHERE deleted = 0;
  SELECT COUNT(*) FROM report_car_out WHERE deleted = 0;
  ```
- [ ] 测试数据已准备（可选）
- [ ] 索引已创建（推荐）
  ```sql
  -- 运行 boot/src/main/resources/sql/optimize_vehicle_records_indexes.sql
  ```

### 3. 代码准备

- [ ] 代码已从版本库拉取
  ```bash
  git pull origin main
  ```
- [ ] 代码版本正确
  ```bash
  git log --oneline -1
  ```
- [ ] 配置文件已更新
  - [ ] application.yml（后端）
  - [ ] .env（前端）

### 4. 依赖检查

**后端：**
- [ ] Maven依赖已下载
  ```bash
  cd boot && mvn dependency:resolve
  ```
- [ ] pom.xml无冲突

**前端：**
- [ ] npm依赖已安装
  ```bash
  cd vue-big-screen-master && npm install
  ```
- [ ] package.json无冲突

---

## 🔧 部署步骤

### 阶段1：后端部署

#### 1.1 编译打包

- [ ] 进入后端目录
  ```bash
  cd boot
  ```
- [ ] 清理旧的编译文件
  ```bash
  mvn clean
  ```
- [ ] 编译打包（跳过测试）
  ```bash
  mvn package -DskipTests
  ```
- [ ] 检查JAR文件生成
  ```bash
  ls -lh target/*.jar
  ```

#### 1.2 备份旧版本

- [ ] 停止现有服务
  ```bash
  # 根据实际情况使用以下命令之一
  kill -15 $(cat app.pid)
  # 或
  systemctl stop parking-manage
  ```
- [ ] 备份旧JAR文件
  ```bash
  cp parking-manage.jar parking-manage.jar.backup.$(date +%Y%m%d)
  ```
- [ ] 备份配置文件
  ```bash
  cp application.yml application.yml.backup.$(date +%Y%m%d)
  ```

#### 1.3 部署新版本

- [ ] 复制新JAR到部署目录
  ```bash
  cp target/parking-manage.jar /opt/parking-manage/
  ```
- [ ] 更新配置文件（如需要）
- [ ] 检查文件权限
  ```bash
  chmod +x parking-manage.jar
  ```

#### 1.4 启动服务

- [ ] 启动后端服务
  ```bash
  # 方式1：直接启动
  nohup java -jar parking-manage.jar > logs/app.log 2>&1 &
  echo $! > app.pid
  
  # 方式2：使用systemd
  systemctl start parking-manage
  ```
- [ ] 等待服务启动（30-60秒）
- [ ] 检查进程运行
  ```bash
  ps aux | grep parking-manage
  ```
- [ ] 检查端口监听
  ```bash
  netstat -tlnp | grep 8080
  ```

#### 1.5 验证后端

- [ ] 检查启动日志
  ```bash
  tail -f logs/application.log
  ```
- [ ] 查找启动成功标识
  ```
  Started ParkingManageApplication in XX seconds
  ```
- [ ] 测试健康检查
  ```bash
  curl http://localhost:8080/actuator/health
  ```
- [ ] 测试统计接口
  ```bash
  curl -X POST http://localhost:8080/parking/analysis/visitor-vip/statistics \
    -H "Content-Type: application/json" \
    -d '{"parkName":"东区停车场","timeRange":"daily"}'
  ```
- [ ] 验证返回数据
  - [ ] 有数据：返回data对象
  - [ ] 无数据：返回data=null

---

### 阶段2：前端部署

#### 2.1 编译打包

- [ ] 进入前端目录
  ```bash
  cd vue-big-screen-master
  ```
- [ ] 清理旧的编译文件
  ```bash
  rm -rf dist
  ```
- [ ] 编译打包
  ```bash
  npm run build
  ```
- [ ] 检查dist目录生成
  ```bash
  ls -lh dist
  ```

#### 2.2 备份旧版本

- [ ] 备份旧的前端文件
  ```bash
  cd /var/www/html
  mv parking-datav parking-datav.backup.$(date +%Y%m%d)
  ```

#### 2.3 部署新版本

- [ ] 复制dist到Web服务器
  ```bash
  cp -r dist /var/www/html/parking-datav
  ```
- [ ] 检查文件权限
  ```bash
  chown -R www-data:www-data /var/www/html/parking-datav
  chmod -R 755 /var/www/html/parking-datav
  ```

#### 2.4 配置Web服务器

**Nginx配置示例：**
- [ ] 更新nginx配置
  ```nginx
  server {
      listen 80;
      server_name your-domain.com;
      
      location / {
          root /var/www/html/parking-datav;
          index index.html;
          try_files $uri $uri/ /index.html;
      }
      
      location /parking/ {
          proxy_pass http://localhost:8080/parking/;
          proxy_set_header Host $host;
          proxy_set_header X-Real-IP $remote_addr;
      }
  }
  ```
- [ ] 测试nginx配置
  ```bash
  nginx -t
  ```
- [ ] 重启nginx
  ```bash
  systemctl reload nginx
  ```

#### 2.5 验证前端

- [ ] 访问前端页面
  ```
  http://your-domain.com
  ```
- [ ] 检查页面加载
  - [ ] 页面正常显示
  - [ ] 无JavaScript错误
  - [ ] 样式正常
- [ ] 检查浏览器控制台
  - [ ] 无HTTP错误
  - [ ] API请求正常
  - [ ] 日志正常输出

---

## 🧪 功能测试

### 测试场景1：有真实数据

- [ ] 确认数据库有数据
- [ ] 访问统计页面
- [ ] 检查控制台日志：`✅ 使用真实数据`
- [ ] 验证数据显示正确
- [ ] 验证图表渲染正常
- [ ] 测试时间范围切换
  - [ ] 今日
  - [ ] 本周
  - [ ] 本月
  - [ ] 今年

### 测试场景2：无真实数据

- [ ] 清空测试数据或使用空时间范围
- [ ] 访问统计页面
- [ ] 检查控制台日志：`⚠️ 使用模拟数据`
- [ ] 验证模拟数据显示
- [ ] 验证页面无错误
- [ ] 验证用户体验良好

### 测试场景3：详细统计弹窗

- [ ] 点击统计图表
- [ ] 验证弹窗打开
- [ ] 验证VIP类型分布显示
- [ ] 验证访客类型分布显示
- [ ] 验证数据准确性
- [ ] 关闭弹窗测试

### 测试场景4：网络异常

- [ ] 停止后端服务
- [ ] 刷新前端页面
- [ ] 检查控制台日志：`❌ 请求失败`
- [ ] 验证降级为模拟数据
- [ ] 验证页面不崩溃
- [ ] 恢复后端服务

### 测试场景5：并发访问

- [ ] 多个用户同时访问
- [ ] 检查服务器CPU/内存
- [ ] 验证响应时间
- [ ] 检查是否有错误日志

---

## 📊 性能验证

### 后端性能

- [ ] 接口响应时间 < 200ms
  ```bash
  time curl -X POST http://localhost:8080/parking/analysis/visitor-vip/statistics \
    -H "Content-Type: application/json" \
    -d '{"parkName":"东区停车场","timeRange":"daily"}'
  ```
- [ ] CPU使用率 < 50%
  ```bash
  top | grep java
  ```
- [ ] 内存使用 < 2GB
  ```bash
  ps aux | grep java | awk '{print $6}'
  ```
- [ ] 数据库查询时间 < 100ms
  ```sql
  -- 在MySQL中执行
  SET profiling = 1;
  SELECT * FROM report_car_in WHERE deleted = 0 LIMIT 50;
  SHOW PROFILES;
  ```

### 前端性能

- [ ] 页面加载时间 < 3秒
  - 打开浏览器开发者工具 → Network
- [ ] 首屏渲染时间 < 2秒
- [ ] 图表渲染流畅
- [ ] 交互响应迅速

---

## 🔍 监控设置

### 日志监控

- [ ] 配置日志滚动
  ```xml
  <!-- logback.xml -->
  <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <fileNamePattern>logs/app.%d{yyyy-MM-dd}.log</fileNamePattern>
      <maxHistory>30</maxHistory>
  </rollingPolicy>
  ```
- [ ] 配置日志级别
  ```yaml
  logging:
    level:
      com.parkingmanage: INFO
  ```
- [ ] 测试日志输出
  ```bash
  tail -f logs/application.log | grep "访客VIP"
  ```

### 告警设置

- [ ] 配置错误告警
- [ ] 配置性能告警
- [ ] 配置可用性监控
- [ ] 测试告警通知

### 指标监控

- [ ] API调用次数
- [ ] API响应时间
- [ ] 错误率
- [ ] 系统资源使用

---

## 📝 文档确认

### 技术文档

- [ ] [后端重构文档](boot/VISITOR_VIP_ANALYSIS_REFACTOR_SUMMARY.md)
- [ ] [前端适配文档](vue-big-screen-master/FRONTEND_ADAPTER_GUIDE.md)
- [ ] [前后端对照表](FULL_STACK_CHANGES_SUMMARY.md)
- [ ] [快速参考卡片](QUICK_REFERENCE_CARD.md)
- [ ] [测试指南](boot/QUICK_TEST_GUIDE.md)

### 运维文档

- [ ] 部署流程文档
- [ ] 回滚流程文档
- [ ] 故障处理文档
- [ ] 监控告警文档

---

## 🚨 回滚计划

### 触发条件

- [ ] 严重功能错误
- [ ] 性能严重下降
- [ ] 数据错误
- [ ] 用户无法使用

### 回滚步骤

#### 后端回滚

1. [ ] 停止新版本服务
   ```bash
   systemctl stop parking-manage
   ```
2. [ ] 恢复旧版本JAR
   ```bash
   cp parking-manage.jar.backup.YYYYMMDD parking-manage.jar
   ```
3. [ ] 恢复配置文件
   ```bash
   cp application.yml.backup.YYYYMMDD application.yml
   ```
4. [ ] 启动旧版本
   ```bash
   systemctl start parking-manage
   ```
5. [ ] 验证服务正常

#### 前端回滚

1. [ ] 恢复旧版本文件
   ```bash
   rm -rf /var/www/html/parking-datav
   cp -r parking-datav.backup.YYYYMMDD /var/www/html/parking-datav
   ```
2. [ ] 重启Web服务器
   ```bash
   systemctl reload nginx
   ```
3. [ ] 验证页面正常

#### 数据库回滚

1. [ ] 恢复数据库备份（如有必要）
   ```bash
   mysql -u root -p parking_db < backup_YYYYMMDD.sql
   ```

---

## ✅ 部署完成检查

### 功能完整性

- [ ] 统计分析功能正常
- [ ] 详细统计功能正常
- [ ] 图表显示正常
- [ ] 时间范围切换正常
- [ ] 数据刷新正常

### 性能指标

- [ ] 响应时间达标
- [ ] CPU使用率正常
- [ ] 内存使用正常
- [ ] 数据库性能正常

### 用户体验

- [ ] 页面加载速度快
- [ ] 交互响应流畅
- [ ] 无明显卡顿
- [ ] 数据显示准确

### 稳定性

- [ ] 连续运行1小时无错误
- [ ] 日志无异常
- [ ] 内存无泄漏
- [ ] 并发访问正常

---

## 📞 联系信息

### 技术支持

| 角色 | 姓名 | 电话 | 邮箱 |
|-----|------|------|------|
| 项目负责人 | _______ | _______ | _______ |
| 后端开发 | _______ | _______ | _______ |
| 前端开发 | _______ | _______ | _______ |
| 运维工程师 | _______ | _______ | _______ |
| DBA | _______ | _______ | _______ |

### 紧急联系

- **值班电话：** _____________
- **微信群：** _____________
- **钉钉群：** _____________

---

## 📋 部署签字

### 部署执行

| 角色 | 姓名 | 签字 | 日期 |
|-----|------|------|------|
| 部署负责人 | _______ | _______ | _______ |
| 后端开发 | _______ | _______ | _______ |
| 前端开发 | _______ | _______ | _______ |
| 测试人员 | _______ | _______ | _______ |

### 验收确认

| 角色 | 姓名 | 签字 | 日期 |
|-----|------|------|------|
| 项目经理 | _______ | _______ | _______ |
| 技术负责人 | _______ | _______ | _______ |
| 产品负责人 | _______ | _______ | _______ |
| 运维负责人 | _______ | _______ | _______ |

---

## 📝 部署记录

### 部署过程记录

```
时间：_____________
环境：_____________
版本：_____________

部署步骤：
1. _______________________________
2. _______________________________
3. _______________________________

遇到的问题：
1. _______________________________
   解决方案：_______________________________

2. _______________________________
   解决方案：_______________________________

备注：
_______________________________________
_______________________________________
```

### 验证结果记录

```
功能测试：□ 通过  □ 不通过
性能测试：□ 通过  □ 不通过
安全测试：□ 通过  □ 不通过
压力测试：□ 通过  □ 不通过

详细说明：
_______________________________________
_______________________________________
_______________________________________
```

---

## 🎉 部署完成

- [ ] 所有检查项已完成
- [ ] 所有测试已通过
- [ ] 文档已更新
- [ ] 团队已通知
- [ ] 用户已通知（如需要）

**恭喜！部署成功！** 🚀

---

**文档版本：** v1.0.0  
**创建日期：** 2024年10月25日  
**最后更新：** _______________

